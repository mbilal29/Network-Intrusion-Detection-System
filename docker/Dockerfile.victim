FROM alpine:latest

# Install basic networking tools and simple HTTP server
RUN apk add --no-cache \
    python3 \
    py3-pip \
    curl \
    bash \
    iptables

# Default command: interactive shell
CMD ["/bin/bash"]
