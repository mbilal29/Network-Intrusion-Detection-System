FROM ubuntu:22.04

# Basic tools for networking and simple services
RUN apt update && apt install -y \
    iputils-ping \
    curl \
    net-tools \
    python3 \
    python3-pip \
    && apt clean && rm -rf /var/lib/apt/lists/*

# Default command: interactive shell
CMD ["/bin/bash"]
